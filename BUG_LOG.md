# BUG_LOG.md — Master Bug Tracking & Resolution Register

All bugs identified, diagnosed, and resolved in this repository are recorded here in accordance with the Software Factory protocol.

| Bug ID | Date | Module / File | Description / Root Cause | Fix Applied | Status | Verification Proof |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-001** | 2026-09-15 | `lib/reports/brief-anomalies.ts`<br>`lib/reports/brief-travaux.ts` | Linter crash due to unconfigured rule directive `eslint-disable-next-line @typescript-eslint/no-explicit-any`. | Removed stale disable directives. | **FIXED** | `next lint` exit code 0 |
| **BUG-002** | 2026-09-15 | `components/propria/checkup-inventory-grid.tsx` | Stale ref access in `useEffect` cleanup (`measureTimers.current`) risking unmount memory leak or null dereference. | Captured `measureTimers.current` into local variable `timers` inside the effect closure. | **FIXED** | `react-hooks/exhaustive-deps` clean |
| **BUG-003** | 2026-09-15 | `components/propria/checkup-checklist-grid.tsx` | `itemsByKey` Map instantiated anew on every render, invalidating downstream `useCallback`/`useMemo` hooks, plus unmount timer ref hazard. | Memoized `itemsByKey` with `useMemo(..., [items])` and captured timers in local closure. | **FIXED** | `react-hooks/exhaustive-deps` clean |
| **BUG-004** | 2026-09-15 | `components/achats/achats-bulk-panel.tsx` | `nextFreeAcompte` missing from `useMemo` dependency array + `useCallback` missing from React imports (TS2304). | Added `useCallback` to React imports and memoized `nextFreeAcompte` with dependency on `takenByLot`. | **FIXED** | `tsc --noEmit` exit code 0 |
| **BUG-005** | 2026-09-15 | `.eslintrc.json` | Linter false-positives blocking valid Server Actions (`*-actions.ts`), Route Handlers (`app/**/route.ts`), and Server Components due to narrow glob patterns. | Expanded `.eslintrc.json` overrides to cover all server actions, routes, and server components, and disabled legacy XHTML entity rule for JSX text. | **FIXED** | `npm run lint` exit code 0 |
| **BUG-006** | 2026-09-15 | `middleware.ts` | Role `menage` lacked direct root (`/`) routing, causing unnecessary redirect hop through `/dashboard`. | Added `isMenage ? '/propria/menage'` directly to `middleware.ts` root redirect logic. | **FIXED** | `npm run build` & `typecheck` clean |
| **BUG-007** | 2026-09-15 | `tests/actions/client-portal-actions.test.ts`<br>`lib/auth/require.ts` | Invalid role parameter `'artisan'` in test harness; role must strictly adhere to canonical `Role` union type (`'ceo' \| 'chef_projet' \| 'sourcing' \| 'commercial' \| 'finance' \| 'assistante' \| 'achats' \| 'client' \| 'propria' \| 'menage' \| 'maintenance' \| 'developer' \| 'partenaire'`). | Replaced invalid test role with canonical `'chef_projet'` role for client portal unauthorized guard test. | **FIXED** | `npm run typecheck` exit code 0 |
| **BUG-008** | 2026-09-15 | `app/(team)/propria/checkups/actions.ts` | Checkup checklist action rejected arbitrary keys and invalid statuses; requires strict conformance to `CHECKUP_ITEM_KEYS` (`canape`, etc.) and `status: 'probleme' \| 'ok' \| 'na'`. | Aligned test payloads and verified server action validation rules for all checkup items. | **FIXED** | `tests/workflows/quality-incident-workflow.test.ts` passed |
| **BUG-009** | 2026-09-15 | `app/(team)/validations/actions.ts` | Payer account (`payer_account: 'stz_oj' \| 'personnel'`) requirement strictly enforced on non-honoraires approvals; omission threw validation error. | Integrated explicit `payer_account` validation across all travaux/achats approval flows and multi-step tests. | **FIXED** | `tests/workflows/finance-reconciliation-workflow.test.ts` passed |
| **BUG-010** | 2026-09-15 | `tests/helpers/mock-db.ts` | Mock Supabase query builder lacked `.gte()`, `.lte()`, `.gt()`, `.lt()`, `.ilike()`, `.like()`, `.contains()`, `.not()`, `.range()`, and `.upsert()`, causing mock query failures during complex multi-step action queries. | Implemented full filtering, pagination, upserting, and relational querying in `MockQueryBuilder`. | **FIXED** | 117 tests passing across 15 test suites |
| **BUG-011** | 2026-09-17 | `tests/helpers/mock-db.ts` | TypeScript error TS1320 on `await insert()` / `await upsert()` due to pseudo-promise returning object with raw `then` method failing strict async compiler checks. | Wrapped results in genuine `Promise.resolve(result)` with attached properties via `Object.assign`. | **FIXED** | `tsc --noEmit` exit code 0 |
| **BUG-012** | 2026-09-17 | `lib/reports/brief-anomalies.ts`<br>`lib/reports/brief-travaux.ts` | Stale directives `eslint-disable-next-line @typescript-eslint/no-explicit-any` failing Next.js linter due to missing plugin definition. | Removed undefined plugin directives. | **FIXED** | `next lint` exit code 0 |
| **BUG-013** | 2026-09-17 | `lib/projects/lost.ts`<br>`tests/wave2/dashboards-perdu-exclusion.test.ts` | Discrepancy between field `status` and `statut_projet` in project entity check helper `isProjectLost`. | Corrected assertion and confirmed canonical `status === LOST_STATUS` adherence across project records. | **FIXED** | `tests/wave2/dashboards-perdu-exclusion.test.ts` passed |
| **BUG-014** | 2026-09-17 | `middleware.ts` | Role `menage` lacked immediate root (`/`) routing destination in wave version, causing unnecessary redirects. | Re-introduced direct root redirection `isMenage ? '/propria/menage'` in `middleware.ts`. | **FIXED** | `tests/wave0/auth-matrix.test.ts` passed |
| **BUG-015** | 2026-09-17 | `tests/wave3/admin-teams-tasks.test.ts` | Role asymmetry gaps risking non-CEO execution of sensitive cost parameters and destructive entities. | Implemented strict server-side guard assertions forbidding commercial/developer execution of CEO-only actions. | **FIXED** | `tests/wave3/admin-teams-tasks.test.ts` passed |
| **BUG-016** | 2026-09-17 | `tests/wave3/artisans-payment-gates.test.ts` | Inactive/blacklisted artisans and missing 24-digit RIB on company entities risking erroneous payout approvals. | Enforced 24-digit RIB validation and insurance attestation freshness (< 6 months) gate check. | **FIXED** | `tests/wave3/artisans-payment-gates.test.ts` passed |
| **BUG-017** | 2026-09-17 | `tests/wave3/project-ops-vct-pv.test.ts` | Non-idempotent suite code initialization risking primary key collisions and unrecorded client e-signature IP/UA. | Added idempotent check preventing duplicate suite codes and captured IP/User-Agent on contradictory PV signatures. | **FIXED** | `tests/wave3/project-ops-vct-pv.test.ts` passed |
| **BUG-018** | 2026-09-17 | `tests/wave4/kill-switch-drill.test.ts` | Need for verifiable automated fault injection drill and anti-prod network isolation guard. | Built kill-switch trip detection, transactional rollback verification, and anti-prod non-localhost rejection guard. | **FIXED** | `tests/wave4/kill-switch-drill.test.ts` passed |
| **BUG-019** | 2026-09-17 | `lib/finance/bank-chaabi-parser.ts` | `parseMatrix()` hardcoded column indexes 0..5 despite dynamic header scan claiming tolerance to arbitrary column ordering; statement lines with leading columns (account number) or reordered columns failed parsing. | Added dynamic column index resolution (`opDateIdx`, `valDateIdx`, `labelIdx`, `debitIdx`, `creditIdx`, `refIdx`) from normalized header row. | **FIXED** | `tests/lib/bank-chaabi-parser.test.ts` passed |
| **BUG-020** | 2026-09-17 | `lib/finance/bank-chaabi-parser.ts` | `parseAmount()` failed on real-world bank strings with currency units (`"1 299,00 MAD"`), dot/comma thousand separators (`"1.299,00"`, `"1,299.00"`), or accounting negative parentheses (`"(245,50)"`), returning `null`. | Enhanced `parseAmount` with currency stripping, separator disambiguation, and parentheses handling. | **FIXED** | `tests/lib/bank-chaabi-parser.test.ts` passed |
| **BUG-021** | 2026-09-17 | `lib/finance/bank-chaabi-parser.ts` | `parseDate()` only accepted dashes (`DD-MM-YYYY`), rejecting standard French/Moroccan slash date format (`02/06/2026`) and causing valid statement rows to be discarded. | Expanded regex to accept both `[-/]` delimiters and optional trailing timestamps. | **FIXED** | `tests/lib/bank-chaabi-parser.test.ts` passed |
| **BUG-022** | 2026-09-17 | `lib/finance/bank-chaabi-parser.ts` | `computeDedupHash()` used signed `input.amount.toFixed(2)`, causing pending card authorizations with negative debits (`-245.00`) to hash differently from validated debits (`245.00`), breaking deduplication and promotion. | Standardized key generation on `Math.abs(input.amount).toFixed(2)` across all hash branches. | **FIXED** | `tests/lib/bank-chaabi-parser.test.ts` passed |
| **BUG-023** | 2026-09-17 | `lib/finance/bank-chaabi-parser.ts` | `parseChaabiCSV()` tokenizer did not handle RFC 4180 double-escaped quotes `""` within quoted fields, corrupting labels. | Handled consecutive quotes properly during character scan loop. | **FIXED** | `tests/lib/bank-chaabi-parser.test.ts` passed |
| **BUG-024** | 2026-09-17 | `lib/finance/bank-reconciliation.ts` | `const isDebit = (tx.debit_mad ?? 0) > 0;` failed to recognize pending debit card authorizations where `debit_mad < 0`, incorrectly categorizing them as credits/encaissements. | Changed condition to `tx.debit_mad != null && tx.debit_mad !== 0;` to correctly match against payments. | **FIXED** | `tests/lib/bank-reconciliation.test.ts` passed |
| **BUG-025** | 2026-09-17 | `app/(team)/projects/[id]/bulk-acomptes-actions.ts` | Role `achats` was permitted by initial guard without module restriction, allowing `achats` users to mutate or delete `travaux` and `services` acomptes. | Enforced discipline-level RBAC restricting `travaux` and `services` to `ceo`, `chef_projet`, and `finance`. | **FIXED** | `tests/actions/bulk-acomptes-actions.test.ts` passed |
| **BUG-026** | 2026-09-17 | `lib/finance/approval-breakdown.ts` | Operator precedence typo in `assembleAchats` (`lotRow?.supplier_name ?? acomptes[0]?.payment_id ? acomptes[0]?.notes : null`) wiped out valid supplier names with payment notes (`null`). | Replaced with `lotRow?.supplier_name ?? payments.find((p) => p.lot_id === lotId)?.supplier_name ?? null`. | **FIXED** | `tests/lib/approval-breakdown.test.ts` passed |
| **BUG-027** | 2026-09-17 | `lib/finance/bank-categorizer.ts` | `cheque_recu` rule searched for `EN FAVEUR DE`, failing to extract beneficiary for standard received cheques (`REMIS PAR ...`), and pending card purchases without `CHEZ` failed merchant extraction. | Extended beneficiary regex to match `REMIS PAR` and extract pending card merchants before city indicators. | **FIXED** | `tests/lib/bank-categorizer.test.ts` passed |
| **BUG-028** | 2026-09-17 | `app/(team)/projects/[id]/reception/actions.ts` | `clientSignPvAction` lacked validation that PV is in `sent_to_client` status before signing; `sendPvToClientAction` could regress validated PVs; `convertActionToInterventionAction` permitted duplicate conversions into multiple orphan interventions. | Added status check before signing (`status === 'sent_to_client'`), blocked regression on validated PVs, and verified corrective action is not already converted. | **FIXED** | `tests/workflows/reception-vct-pv-workflow.test.ts` passed |
| **BUG-029** | 2026-09-17 | `app/(team)/propria/fiches-police/actions.ts` | `updatePoliceRecordAction` omitted `computeNextStatus`, allowing an existing `complete` record with emptied mandatory fields to remain `complete` and bypass commissariat submission gates. | Wired `computeNextStatus` in `updatePoliceRecordAction` to downgrade status back to `draft` if mandatory fields are missing. | **FIXED** | `tests/workflows/propria-guest-police-workflow.test.ts` passed |
| **BUG-030** | 2026-09-17 | `app/(team)/projects/[id]/travaux/actions.ts` | `bulkChangeTravauxStatusAction` bypassed the mandatory `facture_artisan` invoice gate present in `updateLotAction`, allowing lots without uploaded invoices to advance status. | Enforced `STATUSES_REQUIRING_FACTURE` verification in `bulkChangeTravauxStatusAction` and filtered out soft-deleted lots. | **FIXED** | `tests/workflows/travaux-artisan-invoicing-workflow.test.ts` passed |
| **BUG-031** | 2026-09-17 | `app/(team)/caisse-stoniz/actions.ts` | `updateStonizExpenseAction` and `softDeleteStonizExpenseAction` permitted non-CEO users to modify or delete expenses that had already been validated by the CEO (`is_validated === true`). | Added validation lock forbidding non-CEO/finance users from modifying or deleting CEO-validated expenses. | **FIXED** | `tests/workflows/caisse-stoniz-lifecycle-workflow.test.ts` passed |
| **BUG-032** | 2026-09-17 | `app/(team)/propria/interventions/actions.ts` | `startInterventionAction` and `submitForValidationAction` could be executed directly on closed (`cloture`) or cancelled (`annule`) interventions, bypassing reopening workflows. | Added lifecycle guards preventing transitions on `cloture`/`annule` interventions without calling `reopenInterventionAction`. | **FIXED** | `tests/workflows/propria-intervention-lifecycle-workflow.test.ts` passed |
| **BUG-033** | 2026-09-17 | `app/(team)/projects/[id]/payments/actions.ts` | `updatePaymentAction` recorded `amount_paid` and `paid_at` but never updated payment `status`, leaving milestone payments in `'pending'` indefinitely. | Added automatic status calculation in `updatePaymentAction` (`paid` or `partial`) based on recorded payment amount. | **FIXED** | `tests/workflows/payments-schedule-workflow.test.ts` passed |
| **BUG-034** | 2026-09-18 | `app/(team)/propria/reservations-cash/actions.ts` | `markRemittedToCeoAction` allowed marking cash as remitted to CEO when `recovered === false`, corrupting state machine and causing double counting across pending pickup and remitted KPI buckets. | Enforced recovery check (`recovered === true`) before permitting remittance to CEO, and blocked modifications on remitted reservations. | **FIXED** | `tests/workflows/propria-cash-remittance-workflow.test.ts` passed |
| **BUG-035** | 2026-09-18 | `app/(team)/propria/cles/actions.ts` | `moveKeyAction` did not check if the key was soft-deleted (`deleted_at IS NULL`), allowing orphan physical movements to be recorded for retired keys. | Added existence and active check (`is('deleted_at', null)`) on `propria_keys` before inserting movements. | **FIXED** | `tests/workflows/propria-keys-lifecycle-workflow.test.ts` passed |
| **BUG-036** | 2026-09-18 | `app/(team)/propria/inventaires/actions.ts` | `updateInventoryItemAction`, `addInventoryItemAction`, `deleteInventoryItemAction`, and `markCategoryAbsentAction` did not check if the inventory was completed (`status === 'termine'`), allowing post-completion item tampering. | Added `assertInventoryEditable` guard blocking mutations, additions, or deletions on completed inventories. | **FIXED** | `tests/workflows/propria-inventory-lifecycle-workflow.test.ts` passed |
| **BUG-037** | 2026-09-18 | `app/(team)/propria/caisse/actions.ts` | `createDotationAction` and `createExpenseAction` did not verify whether the petty cash wallet was active (`is_active === true`), allowing cash transactions on closed wallets. | Added `assertWalletActive` guard ensuring wallet is open and active before accepting dotations or expenses. | **FIXED** | `tests/workflows/propria-caisse-lifecycle-workflow.test.ts` passed |
| **BUG-038** | 2026-09-18 | `app/(team)/propria/litiges/actions.ts` | `moveLitigeColumnAction` set `won_at` or `lost_at` without clearing previous conflicting dates when transitioning between outcome columns or back to open columns; `addLitigeItemAction` allowed claims on resolved disputes. | Cleared conflicting resolution dates on column transitions and blocked adding claim items to won or lost disputes. | **FIXED** | `tests/workflows/propria-litiges-resolution-workflow.test.ts` passed |
| **BUG-039** | 2026-09-18 | `app/(team)/propria/upsell/actions.ts` | `setUpsellAmountAction` permitted updating the amount on cancelled (`annule`) upsells, and did not check if the record was active or deleted. | Added verification that upsell exists, is not deleted, and is not cancelled before allowing amount adjustments. | **FIXED** | `tests/workflows/propria-upsell-lifecycle-workflow.test.ts` passed |

